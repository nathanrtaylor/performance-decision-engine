signal_thresholds:

  mode: development   # development | production

  global:
    min_confidence: 0.0
    max_volatility: null
    min_denominator_default: 1

  by_category:
    business:
      min_confidence: 0.0
      min_abs_gap_from_benchmark: 0.0
      min_bad_trend_pct: 0.0

    tool_usage:
      min_confidence: 0.0
      min_abs_gap_from_benchmark: 0.0
      min_bad_trend_pct: 0.0

    quality_behavior:
      min_confidence: 0.0
      min_bad_z: 0.0
      min_bad_trend_pct: 0.0

  candidate_rules:
    require_bad_magnitude: false
    require_bad_trend: false
    only_worsening: false
    require_reference_point: false
    max_candidates_per_agent: 10
