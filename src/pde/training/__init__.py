"""Training-insights domain logic (Phase 3+).

The training domain reuses the shared coaching engine core (scoring, windowing,
gating, selection). This package adds the training-specific program-structure and
remediation logic that the coaching domain does not have.
"""
