# ---- secrets / env ----
.env
.env.*
!.env.example
*.pem
*.key
*service-account*.json

# ---- python ----
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# ---- notebooks ----
.ipynb_checkpoints/

# ---- OS / IDE ----
.DS_Store
Thumbs.db
.vscode/
.idea/
~$*        # Office (Word/Excel) lock/temp files, e.g. ~$Ascend Simulations.xlsx

# ---- generated artifacts ----
outputs/runs/**
outputs/benchmark_recalc/**
outputs/theme_discovery/**
outputs/training_runs/**

# ---- data (generated or external drops) ----
data/raw/**
data/interim/**
data/marts/**
data/**/*.csv
# live training inputs (class roster xlsx) -- PII, refreshed per run, never committed
data/training/**

# keep small samples + fixtures
!data/samples/**

# temporary ignore until we pick up the ASCEND work
extraction/compiled_sql/adhoc_training_assist/**
materials/**

# ---- docs/training working files ----
# Source docs (xlsx/docx/curriculum CSVs) already committed here stay tracked; new untracked
# drops in this dir are ignored (use `git add -f` to track a new one deliberately).
docs/training/